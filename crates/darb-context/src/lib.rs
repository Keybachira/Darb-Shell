//! darb-context — lazy indexer, project map, selector, tokenizer, compression, relevance (§20).

pub mod compression;
pub mod indexer;
pub mod project_map;
pub mod relevance;
pub mod selector;
pub mod tokenizer;
