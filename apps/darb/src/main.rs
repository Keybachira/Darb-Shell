//! darb binary — bootstrap only (§25).
//! Business logic must NOT live in main.rs.

fn main() {
    // Bootstrap: config loading, runtime init, TUI init, shutdown.
    // Delegates to library crates (currently stub).
    println!("Darb Shell v0.1.0 — bootstrap stub (see apps/darb/src/main.rs)");
}
